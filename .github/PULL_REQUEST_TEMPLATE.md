## What does this PR do?

<!-- A short summary of the change. -->

## How was it tested?

<!-- Describe how you verified it in a browser: e.g. started a run, leveled up,
     took a hit, triggered an elite. -->

## Checklist

- [ ] Tested in a browser via a local static server
- [ ] Kept the module boundaries intact (logic does not touch the DOM except
      through `hud.js` / `dom.js`; `render.js` only reads state)
- [ ] No new dependencies or build step
